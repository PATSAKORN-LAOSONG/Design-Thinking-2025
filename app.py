import streamlit as st

st.set_page_config(page_title="ระบบประเมินระดับความดันโลหิต")

st.title("🩺 ระบบประเมินระดับความดันโลหิต")
st.write("กรอกค่าความดันโลหิตของคุณ ระบบจะบอกระดับตามเกณฑ์มาตรฐาน")

# รับค่า
systolic = st.number_input("ค่า SYSTOLIC (ค่าความดันตัวบน)", min_value=0, step=1)
diastolic = st.number_input("ค่า DIASTOLIC (ค่าความดันตัวล่าง)", min_value=0, step=1)

if st.button("ตรวจสอบระดับความดัน"):

    if systolic == 0 or diastolic == 0:
        st.warning("กรุณากรอกค่าความดันให้ครบถ้วน")
    else:

        if systolic < 120 and diastolic < 80:
            st.success("🟢 ระดับปกติ (Normal)")

        elif 120 <= systolic <= 129 and diastolic < 80:
            st.info("🟡 ระดับค่อนข้างสูง (Elevated)")

        elif (130 <= systolic <= 139) or (80 <= diastolic <= 89):
            st.warning("🟠 ความดันโลหิตสูง ระยะที่ 1")

        elif systolic >= 140 or diastolic >= 90:
            st.error("🔴 ความดันโลหิตสูง ระยะที่ 2")

        if systolic > 180 or diastolic > 120:
            st.error("⚠️ ภาวะความดันสูงรุนแรง ควรพบแพทย์ทันที")

st.markdown("---")
st.caption("หากมีอาการเจ็บหน้าอก หายใจลำบาก หรือพูดไม่ชัด ควรรีบไปโรงพยาบาลทันที")
